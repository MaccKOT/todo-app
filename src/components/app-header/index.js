//WebPack всегда ищет в папке файл index.js и по умолчанию импортирует именно его, так что можно не указывать файл для импорта, а дать путь до папки

import AppHeader from "./app-header";

export default AppHeader;
